# Binaries (tracked via git-lfs)
# *.raw and *.img are tracked by git-lfs

*.qcow2

# Large files served via HTTP (tracked via git-lfs)
# http-root/rootfs.img
# http-root/initramfs.img
# http-root/vmlinuz

# Temporary files
*.log
*.swp
*~
.DS_Store

# Build artifacts
my-virt-stack/usr/
